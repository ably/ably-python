[tox]
envlist =
    py{27,31,32,33,34}

[testenv]
passenv = TRAVIS TRAVIS_JOB_ID TRAVIS_BRANCH

deps =
    nose
    coveralls
    -rrequirements.txt

commands=
    nosetests --with-coverage --cover-package=ably -v
    coveralls
