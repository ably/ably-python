language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
sudo: false
install:
  - travis_retry pip install -r requirements-test.txt
script:
  - py.test --tb=short test
after_success:
  - "if [ $TRAVIS_PYTHON_VERSION == '2.7' ]; then pip install coveralls; coveralls; fi"
